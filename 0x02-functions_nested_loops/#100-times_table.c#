#include "holberton.h"
/**
*print_times_table - The main function
*Return: The return value is zero
**/
void print_times_table(int n)
{
	int x,y;
	for (x = 0; x <= n; x++)
	{	
		if (y <= n)
		{
			_putchar(',');
			_putchar(' ');
			_putchar(' ');
			_putchar(x + '0');
		}
	}
}