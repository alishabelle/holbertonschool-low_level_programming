#include "holberton.h"
/**
 *
 */
void print_triangle(int size)
{
	int space, x;

	if (size <= 0)
	{
		_putchar('\n');
	}
	else
	{
		for (space = 0; space < size; space++)
		{
			_putchar(' ');
			for (x = 0; x <= space; x++)
				_putchar('#');
      			_putchar('\n');
		}
	}

}
